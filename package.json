{
  "name": "brainrotbattle",
  "version": "0.1.0",
  "description": "3D Endless Runner with Italian Brainrot characters",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "cap:sync": "npm run build && npx cap sync",
    "cap:android": "npm run cap:sync && npx cap open android",
    "cap:ios": "npm run cap:sync && npx cap open ios"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "vite": "^5.0.0"
  }
}
